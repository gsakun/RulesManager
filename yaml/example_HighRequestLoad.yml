groups:
- name: example
  rules:
  - alert: HighRequestLoad
    annotations:
      info: High Request Load
    expr: rate(http_request_total}[5m]) > 1000
    for: 1m
    labels:
      severity: warning
